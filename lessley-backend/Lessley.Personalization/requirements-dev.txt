# Test-only dependencies. Kept out of requirements.txt so they are never baked into the
# runtime image (.dockerignore also excludes tests/). Install alongside the app:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.2
