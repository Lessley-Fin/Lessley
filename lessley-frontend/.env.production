# Production build. The SPA is served by Caddy on the SAME origin as the API, so all
# gateway calls are relative (/api/..., /hubs/...). Leave the gateway URL empty.
# The client talks ONLY to the gateway — it never addresses Personalization.
VITE_API_GATEWAY_URL=
