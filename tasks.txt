משימות PUSH NOTIFICATIONS

1. UserController should not contain logic ! create a userService and move all logic their
2. create a skill that explaining code architecture: repository = db connections and crud, service = should contain the logic, controler = should call the service and do not handle logic
3. communicate between gateway&personalization should be: calculation processes - masstransit, get api - http:

http:
get_mcc
get_mcc_by_id
get_user_access_token
get_user_accounts
get_user_transactions_by_account
get_user_transactions


mass-transit:
calculate_club_categories
calculate_user_categories
calculate_top_accounts
calculate_top_stores
calculate_missed_savings
calculate_matching_clubs

4. personalization&gateway should publish finished messages like FinishHandlingEvent or something. so E2E tests can consume this messages and test finish message
5. FinishHandlingEvent should contain: notificationId, type, RecipientCount, requestId, etc. masstransit should add requestId or traceId to understand which request is this. (so i can know which request is this)
6. every calc should be a new event, and each event should be a different notification for a user in the db.
7. i want to have a e2e tests that include a real socket connections, rabbitmq, and mongodb that tests each scenarios. happy and unhappy. i prefer a good 5-10 tests like this that test every flow end-to-end instead of a 50 behavioral tests.
8. each notification needs to include a *list* of categories instead of: list of notification that each one referes to a specific category. user should get only one notification for each deal, even if it has 2 categories which he registered to.
