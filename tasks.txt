PUSH NOTIFICATIONS tasks

i want you to act as a senior full stack developer and a security expert.
i want to create a proxy so the UI can never reach the other services like personalization.
i want you to add a proxy layer in the gateway, while updating the controllers order, requests, etc.
do not change the business logic at all.
you can add validation in controllers but services should not be touched.

1. gateway should be able to serve as a proxy to the personalization for actions that are not mass-transit (the pure http)
2. create an admin only controller that takes all the requests that they are admin only.
3. make sure that the controllers names and requests are in the correct controller logically.
4. make sure that "development only" that should not be on prod or an "admin only" are well protected with the bootstrap user only and admin creds or something.
5. update the corresponding behavior and security policy changes in the personalization either. make is production ready without touching the business layer at all !
i suggest the following controllers:

1. authcontroller (no admin roles controller):
Register
Login
Refresh
GetMyProfile

2. notificationcontroller (should not get a specific role, but should use the bearer to fetch the email):
MarkAsRead
MarkAllAsRead
GetUserNotifications
GetUserConnectionStatus
GetUserGroups (new one - i want to get the groups that taken from signalR)

3. usercontroller (should not get a specific role, but should use the bearer to fetch the email):
CreateNewOpenFinanceConnection (should be named /init)
RecalculateCategories
GetCategories (fetch from db)
GetTransactions (calls http personalization)
GetTransactionByAccount (calls http personalization)
GetAccounts (calls http personalization)
UpdateUser

4. openfonance controller:
should be deleted and `CreateNewOpenFinanceConnection` should move to usercontroller as mentioned

5. admin only controller:
Bootstrap
ChangeUserRole
UpdateUserTags
SendNotificationToGroup
SendNotificationToUser